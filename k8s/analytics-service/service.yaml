apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: project-submission-portal
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: analytics-service
