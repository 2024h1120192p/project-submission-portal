apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: paper-submission-portal
  labels:
    app: users-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: users-service
