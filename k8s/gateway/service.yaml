apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: paper-submission-portal
  labels:
    app: gateway
spec:
  type: LoadBalancer  # Or NodePort for local development
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: gateway
