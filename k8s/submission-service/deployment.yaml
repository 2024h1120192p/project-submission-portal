apiVersion: apps/v1
kind: Deployment
metadata:
  name: submission-service
  namespace: paper-submission-portal
  labels:
    app: submission-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: submission-service
  template:
    metadata:
      labels:
        app: submission-service
    spec:
      # ISSUE #2: Use Workload Identity service account for Cloud SQL access
      serviceAccountName: cloudsql-sa
      containers:
        - name: submission-service
          image: h20240192/ss_g527.paper-submission-portal@sha256:acb9f44124e9ebfc66649e770c22f259f6e4dedb869d166188fceab9da83532d
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
        # Cloud SQL Auth Proxy sidecar container
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
          args:
            - "--structured-logs"
            - "--port=5432"
            - "paper-submission-portal:us-central1:dev-pg"
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
