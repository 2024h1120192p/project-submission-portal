apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: paper-submission-portal
data:
  # Add your application configuration here
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  
  # Cloud SQL Connection (ISSUE #2 fix)
  # Format: project:region:instance
  # Replace with actual value from: terraform output cloudsql_instance_connection_name
  CLOUDSQL_CONNECTION_NAME: "paper-submission-portal:us-central1:dev-pg"
